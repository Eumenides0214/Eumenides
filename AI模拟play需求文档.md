# AI模拟play 需求文档

## 1. 项目概述

### 1.1 项目名称
AI模拟play

### 1.2 项目简介
AI模拟play 是一个附带 AI 功能的聊天室应用。用户可以自行创建不同的虚拟角色，并通过文字或语音的方式与这些角色进行互动。系统支持多角色管理，用户可以自由选择与不同的角色进行聊天或语音通话，获得沉浸式的角色扮演体验。

### 1.3 项目目标
- 提供灵活的角色创建与管理功能
- 通过 AI 自动生成角色头像，降低用户创作门槛
- 支持文字与语音双模态的聊天交互
- 支持与角色进行实时语音通话
- 提供多角色切换与选择功能，丰富用户体验

---

## 2. 用户角色

| 角色 | 描述 |
|------|------|
| 注册用户 | 可以创建角色、管理角色、与角色聊天及语音通话 |
| 访客 | 仅可浏览功能介绍，需注册后使用 |

---

## 3. 功能需求

### 3.1 用户账号管理

#### 3.1.1 注册与登录
- 支持账号密码注册与登录
- 支持第三方账号登录（可选）
- 支持忘记密码、重置密码功能

#### 3.1.2 个人中心
- 查看与编辑个人资料
- 查看我的角色列表
- 查看历史聊天记录
- 账号设置（通知、隐私等）

---

### 3.2 角色管理

#### 3.2.1 角色创建
用户可创建自定义角色，需填写以下信息：

| 字段 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| 角色名称 | 文本 | 是 | 角色的显示名称，支持 2-20 字符 |
| 性别 | 枚举 | 是 | 男 / 女 |
| 年龄 | 数字 | 是 | 支持范围 1-150 |
| 声音类型 | 枚举 | 是 | 如：成熟男声、温柔女声、少年音、萝莉音、磁性男声等 |
| 角色描述 | 长文本 | 是 | 描述角色性格、背景、说话风格等，最多 2000 字符 |
| 标签 | 多选标签 | 否 | 如：暖心、幽默、高冷、知性、傲娇等，便于分类与筛选 |

#### 3.2.2 AI 头像自动生成
- 用户提交角色信息后，系统调用 AI 图像生成服务，根据角色名称、性别、年龄、角色描述、标签等信息自动生成角色头像
- 生成过程显示加载状态
- 生成完成后，用户可选择重新生成（次数限制可配置）
- 用户可选择上传自定义头像替换 AI 生成的头像

#### 3.2.3 角色编辑
- 用户可随时编辑已创建角色的所有信息
- 编辑角色描述或标签后，可重新生成 AI 头像

#### 3.2.4 角色删除
- 支持删除角色，删除前需二次确认
- 删除角色后，相关聊天记录一并清除

#### 3.2.5 角色列表与选择
- 以卡片形式展示用户创建的所有角色，显示头像、名称、标签
- 支持按标签筛选、按名称搜索
- 点击角色卡片即可进入与该角色的聊天界面

---

### 3.3 聊天功能

#### 3.3.1 文字聊天
- 用户可输入文字消息发送给角色
- 角色根据其角色描述、性格、标签等设定，由 AI 生成符合角色人设的回复
- 支持表情、emoji 输入
- 支持发送图片（可选）

#### 3.3.2 语音消息（用户端）
- 用户可按住录音按钮录制语音消息
- 录制完成后可试听、重录或发送
- 发送后系统将语音转文字供 AI 理解，并生成角色回复

#### 3.3.3 角色回复模态（随机）
- 角色 AI 回复时，**随机**以以下两种形式之一返回：
  - **文字形式**：直接以文字气泡展示
  - **语音形式**：根据角色的声音类型设定，将回复内容合成为语音播放，同时展示文字内容
- 随机概率可由系统配置（默认各 50%）
- 用户可在设置中固定角色回复模态（可选）

#### 3.3.4 聊天历史
- 自动保存聊天记录
- 支持查看历史消息
- 支持清空当前角色的聊天记录
- 上下文记忆：AI 基于近 N 轮对话上下文生成回复

---

### 3.4 语音通话功能

#### 3.4.1 发起通话
- 在角色聊天界面提供"语音通话"入口
- 点击后向角色发起语音通话请求
- 显示通话呼叫中状态

#### 3.4.2 通话过程
- 角色接听后进入实时语音通话
- 用户说话，系统实时识别并转换为文字供 AI 处理
- AI 生成回复后，以角色的声音类型合成语音实时播放
- 支持通话中的文字补充输入（可选）

#### 3.4.3 通话控制
- 支持静音/取消静音
- 支持挂断通话
- 通话过程中显示通话时长
- 通话结束后可生成通话摘要（可选）

---

### 3.5 多角色互动

#### 3.5.1 角色切换
- 用户可在不同角色之间自由切换聊天
- 每个角色的聊天记录相互独立
- 切换角色时保留各自的对话上下文

#### 3.5.2 角色广场（可选）
- 提供公共角色广场，用户可将自创角色分享
- 其他用户可体验分享的角色
- 支持点赞、收藏、使用次数统计

---

## 4. 非功能需求

### 4.1 性能需求
| 指标 | 要求 |
|------|------|
| AI 文字回复响应时间 | ≤ 3 秒 |
| AI 头像生成时间 | ≤ 30 秒 |
| 语音识别延迟 | ≤ 2 秒 |
| 语音合成播放延迟 | ≤ 2 秒 |
| 语音通话端到端延迟 | ≤ 1.5 秒 |
| 并发支持 | 支持 1000+ 用户同时在线 |

### 4.2 可用性需求
- 界面简洁直观，操作流程符合用户习惯
- 关键操作（创建、删除、通话）有明确反馈与引导
- 支持移动端与 PC 端响应式布局
- 无障碍支持（可选）

### 4.3 安全与隐私
- 用户数据加密存储
- 聊天内容仅用户本人可见
- 语音数据在传输过程中加密
- 符合相关数据保护法规（如 GDPR、个保法等）
- 角色内容需经过敏感词过滤，禁止违规内容

### 4.4 可扩展性
- 模块化设计，便于后续扩展视频通话、角色表情等功能
- AI 服务接口可替换（支持不同大模型与 TTS/ASR 服务）
- 支持多语言扩展

### 4.5 稳定性
- 系统可用性 ≥ 99.5%
- 具备服务降级与容错机制
- AI 服务不可用时提供友好提示与重试机制

---

## 5. 技术架构

### 5.1 整体架构
采用前后端分离架构，主要包括：

- **前端**：基于 Vue 的 Web 客户端，负责 UI 展示与用户交互
- **后端**：基于 Node.js 与 JavaScript 的业务服务，负责用户、角色、消息管理
- **AI 服务层**：集成 LLM（对话）、TTS（语音合成）、ASR（语音识别）、图像生成（头像）等能力
- **存储层**：MySQL 关系数据库（用户/角色/消息）、对象存储（头像/语音文件）、缓存（会话上下文）

### 5.2 核心技术选型

| 模块 | 技术方案 |
|------|----------|
| 前端框架 | Vue 3 + Vite |
| 前端语言 | JavaScript |
| 前端状态管理 | Pinia |
| 前端路由 | Vue Router |
| 前端 UI 组件库 | Element Plus / Vant（移动端） |
| 后端运行环境 | Node.js |
| 后端语言 | JavaScript |
| 后端框架 | Express / Koa |
| 数据库 | MySQL |
| 数据库访问 | Sequelize / Prisma（MySQL ORM） |
| 缓存 | Redis |
| 对象存储 | OSS / S3 |
| LLM 对话 | 接入主流大模型 API |
| TTS 语音合成 | 主流 TTS 服务，支持多音色 |
| ASR 语音识别 | 主流 ASR 服务 |
| 图像生成 | Stable Diffusion / DALL·E 等 |
| 实时通信 | WebSocket（socket.io）/ WebRTC（语音通话） |

---

## 6. 业务流程

### 6.1 角色创建流程
```
用户填写角色信息 → 提交 → 后端保存角色信息
                     ↓
            调用 AI 图像生成服务
                     ↓
           生成头像 → 返回前端展示
                     ↓
        用户确认 / 重新生成 / 上传自定义头像
                     ↓
              保存角色，创建完成
```

### 6.2 文字/语音聊天流程
```
用户输入（文字/语音）
        ↓
[语音] → ASR 转文字
        ↓
后端组装上下文 → 调用 LLM 生成回复
        ↓
   随机决定回复模态
   ├── 文字 → 直接返回展示
   └── 语音 → TTS 合成 → 返回语音 + 文字
        ↓
    前端展示并播放
        ↓
   保存聊天记录
```

### 6.3 语音通话流程
```
用户点击语音通话 → 发起呼叫请求
        ↓
角色"接听"（模拟接听动画）
        ↓
进入实时通话循环：
  用户说话 → ASR 实时识别 → LLM 生成回复 → TTS 实时合成 → 播放
        ↓
用户挂断 → 结束通话 → 保存通话记录（可选生成摘要）
```

---

## 7. 接口清单（概要）

### 7.1 用户相关
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录
- `GET /api/user/profile` - 获取用户信息
- `PUT /api/user/profile` - 更新用户信息

### 7.2 角色相关
- `POST /api/characters` - 创建角色
- `GET /api/characters` - 获取角色列表
- `GET /api/characters/{id}` - 获取角色详情
- `PUT /api/characters/{id}` - 更新角色
- `DELETE /api/characters/{id}` - 删除角色
- `POST /api/characters/{id}/avatar/generate` - 生成 AI 头像
- `POST /api/characters/{id}/avatar/upload` - 上传自定义头像

### 7.3 聊天相关
- `POST /api/chat/{characterId}/messages` - 发送消息
- `GET /api/chat/{characterId}/messages` - 获取历史消息
- `DELETE /api/chat/{characterId}/messages` - 清空聊天记录
- `POST /api/chat/{characterId}/voice` - 发送语音消息
- `WebSocket /ws/chat/{characterId}` - 实时聊天通道

### 7.4 语音通话相关
- `POST /api/call/{characterId}/initiate` - 发起语音通话
- `WebSocket /ws/call/{callId}` - 通话实时数据通道
- `POST /api/call/{callId}/end` - 结束通话

---

## 8. 数据模型（概要）

### 8.1 用户表 (users)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| username | VARCHAR | 用户名 |
| password_hash | VARCHAR | 加密密码 |
| avatar | VARCHAR | 头像 URL |
| created_at | TIMESTAMP | 创建时间 |

### 8.2 角色表 (characters)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| user_id | UUID | 所属用户 |
| name | VARCHAR | 角色名称 |
| gender | ENUM | 性别 |
| age | INT | 年龄 |
| voice_type | VARCHAR | 声音类型 |
| description | TEXT | 角色描述 |
| tags | JSON | 标签数组 |
| avatar | VARCHAR | 头像 URL |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 8.3 消息表 (messages)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| character_id | UUID | 关联角色 |
| user_id | UUID | 所属用户 |
| role | ENUM | user / assistant |
| content_type | ENUM | text / voice |
| content | TEXT | 文字内容 |
| media_url | VARCHAR | 语音文件 URL |
| created_at | TIMESTAMP | 创建时间 |

### 8.4 通话记录表 (call_records)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| character_id | UUID | 关联角色 |
| user_id | UUID | 所属用户 |
| duration | INT | 通话时长（秒） |
| started_at | TIMESTAMP | 开始时间 |
| ended_at | TIMESTAMP | 结束时间 |
| summary | TEXT | 通话摘要（可选） |

---

## 9. 版本规划

### V1.0（MVP 核心版本）
- 用户注册登录
- 角色创建（含 AI 头像生成）
- 角色列表与选择
- 文字聊天（含随机语音/文字回复）
- 语音消息发送
- 聊天历史记录

### V1.1
- 语音通话功能
- 角色编辑与删除
- 标签筛选与搜索

### V2.0（扩展版本）
- 角色广场与分享
- 通话摘要生成
- 多语言支持
- 视频通话（可选）
- 角色表情/动作（可选）

---

## 10. 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| AI 回复不符合角色人设 | 用户体验下降 | 优化 Prompt 工程，强化角色设定约束 |
| AI 头像生成质量不稳定 | 角色形象不佳 | 支持多次重新生成与自定义上传 |
| 语音通话延迟过高 | 通话体验差 | 优化流式 ASR/TTS，采用边缘节点加速 |
| AI 服务成本高 | 运营压力大 | 设置合理的使用配额，分级会员机制 |
| 用户生成违规内容 | 合规风险 | 敏感词过滤 + 内容审核机制 |