# 注册登录模块实现

首先就是用户在前端页面填写对应的信息（手机号、验证码、密码）完成注册，然后登陆提交；同时前端验证格式，再把数据传给后端；后端对信息进行核对，通过后就创建用户账号；然后后端生成JWT令牌（携带用户的id）下发给前端；接着前端就保存JWT，通过这个来判断用户登录，然后在后面每次请求的时候都会带上令牌；然后后端就验证JWT解析出用户的id，确认用户身份同时返回数据；最后退出登录的时候，前端就清除本地的令牌，后端令牌作废，登陆状态就失效了。**（写得好烂ovo）**

—————————————————————————————————————————————

## 注册模块

用户在前端页面填写手机号、短信验证码、登录密码，提交注册信息；前端先校验手机号、验证码、密码的格式规范，校验无误后将表单数据发送至后端。

后端核验验证码有效性，确认该手机号尚未注册，校验通过后创建用户账号；随后生成内含用户 ID 的 JWT 令牌返回前端。

前端将 JWT 保存至本地，代表注册成功并自动处于登录状态。

## 登录模块

用户填写手机号 + 密码 / 手机号 + 验证码提交登录，前端校验格式后将数据传递给后端；后端核对账号与凭证信息，验证无误后下发携带用户 ID 的 JWT 令牌。

前端存储 JWT，以此判定用户已登录；后续访问聊天等需要权限的页面时，每次网络请求都会自动携带该令牌。

后端统一校验 JWT 合法性、有效期，解析出用户 ID 确认用户身份，再返回对应业务数据。

退出登录时，前端清空本地存储的令牌，后端将该令牌标记失效，登录状态随即解除。

## JWT格式

三段结构，**英文小数点 `.` 分隔**，无空格，一长串字符串：

```
Header.Base64编码载荷.签名
```

**第一段：Header头部（Base64编码）**

作用：声明加密算法 解码原始JSON

**第二段：Payload载荷（非加密，Base64编码，可解码查看，核心）**

作用：存放用户身份、时效信息（后端通过这个识别用户）

**第三段：Signature签名（安全校验）**

作用：防止篡改令牌（后端用专属密钥 + 算法密钥计算）

如果修改了前两段的任意部分，签名就会校验失败。